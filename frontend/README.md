# Frontend

With the API running at port 8000:

```powershell
npm install
npm run dev
```

This intentionally small UI demonstrates conversational search, visible constraints and the trace. Add image upload, feedback buttons and product comparison after a real catalogue with image assets is available.
