# HTTPS URL printed by the final Colab serving cell (do not include a trailing slash).
FINETUNED_MODEL_URL=https://replace-me.ngrok-free.app
# Must match the private token you set in the Colab notebook.
FINETUNED_MODEL_API_KEY=replace-with-a-long-random-token
PRODUCT_CATALOG_PATH=data/processed/esci_products.json
SESSION_DB_PATH=data/sessions.json
# Persisted FAISS index built by scripts/build_indexes.py. Dense/hybrid requests fail
# explicitly if this file or matching provenance metadata is unavailable.
DENSE_INDEX_PATH=data/indexes/text_embeddings.faiss
